{
  "name": "chat",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel ./src/pages/*.hbs --no-cache",
    "start": "parcel build ./src/pages/*.hbs && node ./server/express.js",
    "build": "parcel build ./src/pages/*.hbs"
  },
  "devDependencies": {
    "@anikin/parcel-transformer-handlebars": "^1.0.9",
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
